import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beatbuddy.app',
  appName: 'BeatBuddy',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
